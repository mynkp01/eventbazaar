import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Our Story at EventBazaar.com - Connecting People and Vendors",
  description:
    "Discover how EventBazaar.com connects users with trusted vendors, simplifies planning, and supports vendors in growing their business across India.",
};

export default function OurStoryLayout({ children }) {
  return children;
}
