import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Explore Talented Artists for Events | Anchors, DJs, VJs & More",
  description:
    "Looking for an artist to light up your event? Find and connect with top anchors, DJs, VJs, and performers all in one place at EventBazaar.com",
};

export default function ArtistLayout({ children }) {
  return children;
}
