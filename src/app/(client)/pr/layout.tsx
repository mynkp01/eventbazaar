import { Metadata } from "next";

export const metadata: Metadata = {
  title: "EventBazaar.com Launch Press Conference | Empowering Event Vendors",
  description:
    "Discover highlights from the EventBazaar.com launch press conference. Learn how our platform is reshaping the future for event vendors with smarter client connections.",
};

export default function PrLayout({ children }) {
  return children;
}
