import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Explore and Compare Event Vendors Seamlessly",
  description:
    "Explore vendor profiles across categories and compare services, reviews, and availability to find your perfect match with EventBazaar.com",
};

export default function CompareLayout({ children }) {
  return children;
}
