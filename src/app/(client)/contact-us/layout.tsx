import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Get in Touch with the EventBazaar.com Team",
  description:
    "Contact EventBazaar.com for inquiries, support, or collaborations. We're here to make your event planning seamless and successful. Reach out today!",
};

export default function ContactUsLayout({ children }) {
  return children;
}
